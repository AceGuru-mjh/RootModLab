# HideAllRoot-System 默认配置
# 1=开启 0=关闭

# 总开关
ENABLE=1

# 属性伪装（service.sh 中 resetprop 伪装 ro.*）
ENABLE_PROP_SPOOF=1

# 系统 mount 视图清理（post-fs-data 卸载 magisk tmpfs 覆盖，保留 /data/adb）
ENABLE_MOUNT_CLEAN=1

# 安装痕迹清理（/cache/magisk*、/data/local/tmp 残留）
ENABLE_TRACE_CLEAN=1
